// goal is to have a parameter that is read when executing from the terminal

int main (int argc, char *argv[]) {
    double M = atof(argv[0]);
    double N = atof(argv[1]);
    double nu = atof(argv[2]);
    double t_end = atof(argv[3]);
    

    return 0;
}